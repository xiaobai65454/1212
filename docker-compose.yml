version: '3.8'

services:
  xiaobaibai:
    build: .
    container_name: xiaobaibai
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    environment:
      - NODE_ENV=production
      - KNOWLEDGE_BASE_PATH=/app/data/knowledge
    volumes:
      - knowledge_data:/app/data/knowledge
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

volumes:
  knowledge_data:
